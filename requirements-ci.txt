# Pin key packages for deterministic integration tests.
# These versions must match the local dev environment so that
# seeded loss values stay consistent across CI and local runs.
torch==2.11.0
torchvision==0.26.0
numpy==2.4.4
timm==1.0.26
transformers==5.4.0
lightning==2.6.1

# fsspec >=2025.3.0 added strict glob validation that rejects the patterns
# huggingface_hub.HfFileSystem.glob() constructs for split-file discovery
# (e.g. ``train[-._ 0-9/]**``), breaking integration tests with
# ``ValueError: Invalid pattern: '**' can only be an entire path component``.
# Pin below that threshold; let huggingface_hub and datasets resolve transitively
# (transformers==5.4.0 requires huggingface_hub>=1.5.0 so we cannot pin hub).
fsspec<2025.3.0
